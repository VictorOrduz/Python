#declarar variables y pedir datos por consola
print("ingrese la produccion por cada turno")
turno1 = float(input("indique la produccion del turno 1: "))
turno2 = float(input("indique la produccion del turno 2: "))

#implementamos operadores
diferencia = turno1 - turno2

print(f"la diferencia de produccion es {diferencia}")
